import { VulnerabilityFinding } from "./api";
export function VulnerabilitiesPage({findings}:{findings:VulnerabilityFinding[]}){return <><header><div><p className="eyebrow">EVIDENCE-BASED FINDINGS</p><h1>Vulnerabilities</h1><p>Exact CPE matches from the NVD; scanner output still requires review.</p></div></header><div className="notice panel">This product uses data from the NVD API but is not endorsed or certified by the NVD.</div>{findings.length===0?<div className="empty-state panel"><h2>No findings yet</h2><p>Inspect a host in Discovery, then run its vulnerability lookup when CPE evidence is available.</p></div>:<div className="finding-grid">{findings.map(f=><article className="panel finding" key={f.id}><div><span className={`severity ${f.severity}`}>{f.severity}</span><b>{f.cve_id}</b><small>CVSS {f.cvss_score||"unscored"}</small></div><p>{f.description}</p><small>{f.address} · {f.cpe}</small></article>)}</div>}</>}
