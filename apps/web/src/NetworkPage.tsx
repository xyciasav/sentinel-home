import { useEffect, useMemo, useState } from "react";
import { api, DiscoveredHost, DiscoveryRun, InventorySource, NetworkAsset, NetworkChange, NetworkIdentityEvent } from "./api";
import { DiscoveryPage } from "./DiscoveryPage";
import { NetworkChangesPage } from "./NetworkChangesPage";
import { SourcesPage } from "./SourcesPage";

export function NetworkPage({sources,run,changes,csrf,refreshSources,refreshDiscovery,refreshDevices,updateHost}:{sources:InventorySource[];run:DiscoveryRun|null;changes:NetworkChange[];csrf:string;refreshSources:()=>Promise<void>;refreshDiscovery:()=>Promise<void>;refreshDevices:()=>Promise<void>;updateHost:(host:DiscoveredHost)=>void}) {
  const [section,setSection]=useState<"overview"|"topology"|"inventory"|"alerts"|"discovery"|"changes">("overview");
  const [assets,setAssets]=useState<NetworkAsset[]>([]),[activity,setActivity]=useState<NetworkIdentityEvent[]>([]),[loading,setLoading]=useState(true);
  async function loadAssets(){setLoading(true);try{setAssets(await api.networkInventory())}finally{setLoading(false)}}
  async function loadActivity(){setActivity(await api.networkActivity())}
  useEffect(()=>{void loadAssets();void loadActivity()},[]);
  const tabs:[typeof section,string][]=[["overview","Overview"],["topology","Topology"],["inventory","Connected inventory"],["alerts",`Alerts${activity.filter(x=>!x.acknowledged_at).length?` (${activity.filter(x=>!x.acknowledged_at).length})`:""}`],["discovery","Discovery"],["changes","Port changes"]];
  return <><div className="network-tabs panel">{tabs.map(([key,label])=><button key={key} className={section===key?"active":""} onClick={()=>{setSection(key);if(key==="overview"||key==="topology")void loadAssets();if(key==="alerts")void loadActivity()}}>{label}</button>)}</div>
    {section==="overview"?<NetworkOverview assets={assets} loading={loading} csrf={csrf} refresh={async()=>{await loadAssets();await refreshDevices()}}/>:section==="topology"?<NetworkTopology assets={assets}/>:section==="inventory"?<SourcesPage sources={sources} csrf={csrf} refresh={async()=>{await refreshSources();await loadAssets()}} refreshDevices={refreshDevices}/>:section==="alerts"?<NetworkAlerts events={activity} csrf={csrf} refresh={loadActivity}/>:section==="discovery"?<DiscoveryPage run={run} csrf={csrf} refresh={refreshDiscovery} refreshDevices={refreshDevices} updateHost={updateHost}/>:<NetworkChangesPage changes={changes}/>}</>;
}

function subnet(address:string|null){if(!address)return "Identity only";const parts=address.split(".");return parts.length===4?`${parts.slice(0,3).join(".")}.0/24`:"Other addresses"}
function NetworkTopology({assets}:{assets:NetworkAsset[]}){
  const groups=useMemo(()=>{const result:Record<string,NetworkAsset[]>={};for(const asset of assets)(result[subnet(asset.address)]??=[]).push(asset);return result},[assets]);
  return <><header><div><p className="eyebrow">NETWORK MAP</p><h1>Topology</h1><p>Assets grouped by observed network segment, with their identity sources and current state.</p></div></header><div className="topology-map"><article className="topology-core panel"><span>SH</span><div><b>Sentinel Home</b><small>{assets.length} correlated assets · {Object.keys(groups).length} segments</small></div></article>{Object.entries(groups).sort().map(([name,items])=><section className="topology-segment panel" key={name}><div className="topology-segment-title"><span/><div><p className="eyebrow">NETWORK SEGMENT</p><h2>{name}</h2></div><b>{items.length}</b></div><div className="topology-nodes">{items.map(item=><article className={!item.linked?"review":item.status==="online"?"online":""} key={item.id}><i/><div><b>{item.name}</b><small>{item.address||item.mac_address||"No routable identity"}</small><span>{item.sources.length?item.sources.join(" + "):"Sentinel"}</span></div></article>)}</div></section>)}</div></>;
}

function NetworkAlerts({events,csrf,refresh}:{events:NetworkIdentityEvent[];csrf:string;refresh:()=>Promise<void>}){
  const [filter,setFilter]=useState<"open"|"all"|"acknowledged">("open"),[busy,setBusy]=useState("");
  const visible=events.filter(item=>filter==="all"||filter==="open"&&!item.acknowledged_at||filter==="acknowledged"&&item.acknowledged_at);
  async function acknowledge(id:string){setBusy(id);try{await api.acknowledgeNetworkActivity(id,csrf);await refresh()}finally{setBusy("")}}
  return <><header><div><p className="eyebrow">NETWORK ATTENTION</p><h1>Network alerts</h1><p>Identity changes stay open until you review them. No port scan noise is mixed into this queue.</p></div><select value={filter} onChange={event=>setFilter(event.target.value as typeof filter)}><option value="open">Needs review</option><option value="all">All alerts</option><option value="acknowledged">Acknowledged</option></select></header><section className="status-grid network-alert-summary"><article className="status-card"><p>Needs review</p><strong>{events.filter(x=>!x.acknowledged_at).length}</strong><small>Unacknowledged identity changes</small></article><article className="status-card"><p>New identities</p><strong>{events.filter(x=>x.kind==="identity_seen"&&!x.acknowledged_at).length}</strong><small>Devices not previously observed</small></article><article className="status-card"><p>Address moves</p><strong>{events.filter(x=>x.kind==="address_changed"&&!x.acknowledged_at).length}</strong><small>Known identities at a new IP</small></article></section>{visible.length===0?<div className="empty-state panel"><div>✓</div><h2>Nothing needs review</h2><p>New network identity changes will appear here.</p></div>:<div className="panel network-alert-list">{visible.map(item=><article className={item.acknowledged_at?"acknowledged":""} key={item.id}><i className={`alert-severity ${item.severity}`}/><div><p className="eyebrow">{item.kind==="identity_seen"?"NEW IDENTITY":"ADDRESS CHANGED"} · {item.severity.toUpperCase()}</p><h2>{item.name}</h2><p>{item.kind==="address_changed"?`${item.old_value||"unknown"} → ${item.new_value}`:`First reported as ${item.new_value||"identity only"}`} by {item.source_name}.</p></div><time>{new Date(item.occurred_at).toLocaleString()}</time>{item.acknowledged_at?<span className="tag">Reviewed</span>:<button disabled={busy===item.id} onClick={()=>void acknowledge(item.id)}>{busy===item.id?"Saving…":"Acknowledge"}</button>}</article>)}</div>}</>;
}

function NetworkOverview({assets,loading,csrf,refresh}:{assets:NetworkAsset[];loading:boolean;csrf:string;refresh:()=>Promise<void>}){
  const [query,setQuery]=useState(""),[filter,setFilter]=useState<"all"|"linked"|"review"|"multi">("all"),[reviewing,setReviewing]=useState<NetworkAsset|null>(null),[target,setTarget]=useState("new"),[busy,setBusy]=useState(false),[error,setError]=useState("");
  const visible=useMemo(()=>assets.filter(item=>{const text=`${item.name} ${item.address||""} ${item.mac_address||""} ${item.sources.join(" ")}`.toLowerCase();return(!query||text.includes(query.toLowerCase()))&&(filter==="all"||filter==="linked"&&item.linked||filter==="review"&&!item.linked||filter==="multi"&&item.sources.length>1)}),[assets,query,filter]);
  const review=assets.filter(item=>!item.linked).length,multi=assets.filter(item=>item.sources.length>1).length,addressed=assets.filter(item=>item.address).length;const canonical=assets.filter(item=>item.linked);
  async function link(){if(!reviewing)return;setBusy(true);setError("");try{await api.linkNetworkIdentity(reviewing.observation_ids,target==="new"?null:target,csrf);setReviewing(null);await refresh()}catch(reason){setError(reason instanceof Error?reason.message:"Could not link identity")}finally{setBusy(false)}}
  return <><header><div><p className="eyebrow">UNIFIED NETWORK IDENTITY</p><h1>Network overview</h1><p>One asset list correlated across Sentinel, Home Assistant, Pi-hole, discovery, and agents.</p></div></header><section className="status-grid"><article className="status-card"><p>Canonical assets</p><strong>{assets.length-review}</strong><small>Tracked in Sentinel</small></article><article className="status-card"><p>Cross-source matches</p><strong>{multi}</strong><small>Confirmed by multiple systems</small></article><article className="status-card"><p>Needs review</p><strong>{review}</strong><small>Observed but not linked</small></article><article className="status-card"><p>Address coverage</p><strong>{addressed}</strong><small>Assets with a known IP</small></article></section><div className="panel network-overview-filters"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search name, IP, MAC, or source"/><select value={filter} onChange={e=>setFilter(e.target.value as typeof filter)}><option value="all">All identities</option><option value="linked">Canonical assets</option><option value="multi">Cross-source matches</option><option value="review">Needs review</option></select><b>{visible.length} shown</b></div>{loading?<div className="empty-state panel"><h2>Correlating network identities…</h2></div>:<div className="panel network-asset-list"><div className="network-asset heading"><span>Asset</span><span>Identity</span><span>Sources</span><span>State</span></div>{visible.map(item=><div className="network-asset" key={item.id}><span><b>{item.name}</b><small>{item.last_seen_at?`Seen ${new Date(item.last_seen_at).toLocaleString()}`:"No recent observation"}</small></span><span><b>{item.address||"No IP yet"}</b><small>{item.mac_address||"Source identity only"}</small></span><span className="source-badges">{item.sources.length?item.sources.map(source=><i key={source}>{source}</i>):<i>Sentinel</i>}</span><span><b className={item.linked?"green":"warning"}>{item.linked?item.status.replace("_"," "):"Needs review"}</b><small>{item.observations} source observation{item.observations===1?"":"s"}</small>{!item.linked&&<button onClick={()=>{setReviewing(item);setTarget("new");setError("")}}>Resolve identity</button>}</span></div>)}</div>}{reviewing&&<div className="modal-backdrop"><div className="panel identity-link-modal"><div className="panel-title"><div><p className="eyebrow">IDENTITY REVIEW</p><h2>{reviewing.name}</h2></div><button className="close" onClick={()=>setReviewing(null)}>×</button></div><p>Observed as <b>{reviewing.address||"no IP"}</b> / <b>{reviewing.mac_address||"no MAC"}</b> by {reviewing.sources.join(", ")}.</p><label>Canonical device<select value={target} onChange={e=>setTarget(e.target.value)}><option value="new">Create a new canonical device</option>{canonical.map(item=><option value={item.id} key={item.id}>{item.name} {item.address?`(${item.address})`:""}</option>)}</select></label><p className="quiet">All {reviewing.observations} grouped source observations will be attached together.</p>{error&&<div className="error">{error}</div>}<div className="form-actions"><button onClick={()=>setReviewing(null)}>Cancel</button><button className="primary compact" disabled={busy} onClick={()=>void link()}>{busy?"Linking…":target==="new"?"Create and link":"Link to device"}</button></div></div></div>}</>;
}
